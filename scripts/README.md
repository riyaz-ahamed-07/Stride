# Scripts

Project-level maintenance tooling (not application runtime).

| Script | Purpose |
|---|---|
| `create-stride-supabase.ps1` | Create a **personal** Supabase project for Stride (does not touch Gatherly). Requires `STRIDE_SUPABASE_ACCESS_TOKEN`. |

API-local helpers live under [`apps/api/scripts/`](../apps/api/scripts/).  
Mobile build helpers live under [`apps/mobile/scripts/`](../apps/mobile/scripts/).
